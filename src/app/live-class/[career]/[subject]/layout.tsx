import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Clase en linea - Halcones en linea',
  description: 'Aplicación de videoconferencia para clases en linea.'
}

export default function Layout ({ children }) {
  return children
}
